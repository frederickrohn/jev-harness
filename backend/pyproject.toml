[project]
name = "jev-harness-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "python-dotenv",
    "typesafe-sdk",
    "uvicorn[standard]",
]

[tool.uv]
package = false
